import { ExperimentLabClient } from './client';
import { ExperimentLabConfig, EventData, Variant } from './types';

export { ExperimentLabClient, ExperimentLabConfig, EventData, Variant };

export default ExperimentLabClient;